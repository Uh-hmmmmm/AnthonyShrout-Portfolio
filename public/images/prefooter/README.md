# PreFooter

Currently reuses `backgrounds/grain-circle.png` for the "Run the Moment" section — no unique assets needed here yet. If any PreFooter-specific graphic is added later, drop it in this folder.
